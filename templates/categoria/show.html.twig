{% extends 'base.html.twig' %}

{% block title %}Categoria{% endblock %}

{% block body %}
    <h1>Categoria</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ categorium.Nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ categorium.Descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categoria_index') }}">Volver a la lista</a>

    <a href="{{ path('app_categoria_edit', {'id': categorium.id}) }}">Editar</a>

    {{ include('categoria/_delete_form.html.twig') }}
{% endblock %}
